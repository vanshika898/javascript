let message = 'vanshika nigam';

var lastIndex = message.lastIndexOf('a');
var index = message.indexOf('a');
console.log(lastIndex);


console.log(`first index of a is ${index} and last index of char a is ${lastIndex}`)