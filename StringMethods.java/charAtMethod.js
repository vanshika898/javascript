let message = "my name is vanshika nigam";

let index =8;
let char = message.charAt(index);
console.log();

let asii = message.charCodeAt(index);

console.log(`so the ascii code of ${char} is ${asii}`);